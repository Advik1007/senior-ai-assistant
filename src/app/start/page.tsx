import { LanguageSelector } from "@/components/LanguageSelector";

/** In-app first screen: choose language, then continue to auth. */
export default function StartPage() {
  return <LanguageSelector />;
}
