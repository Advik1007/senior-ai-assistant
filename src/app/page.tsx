import { LanguageSelector } from "@/components/LanguageSelector";

/** First screen: language list only. No loading, no redirects on this page. */
export default function LanguagePage() {
  return <LanguageSelector />;
}
